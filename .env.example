# Environment (development, production)
NODE_ENV=development

# Server Configuration
PORT=3000

# API Server URL (used by OAuth scripts and HTML helpers)
# Development: https://localhost:3000
# Production: https://mcp-client.owlfort.io
API_SERVER=https://localhost:3000

# SSL/HTTPS Configuration
# Set to 'true' to enable HTTPS, 'false' for HTTP
# In production with Nginx, set to 'false' (Nginx handles SSL)
SSL_ENABLED=true

# SSL Certificate Paths (for local development with self-signed certs)
# In production, these are managed by Let's Encrypt/Certbot
# SSL_CERT_PATH=./certs/cert.pem
# SSL_KEY_PATH=./certs/key.pem
